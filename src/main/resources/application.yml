server:
  port: 8080

spring:
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/realfolio}
    username: ${DATASOURCE_USERNAME:postgres}
    password: ${DATASOURCE_PASSWORD:postgres}
  
  jpa:
    hibernate:
      ddl-auto: validate
  
  flyway:
    enabled: true

yahoo:
  base-url: https://query1.finance.yahoo.com

